Autonomous Agent build requirement list preparation document:



We want to make a TG function that when a user has an account is registered, it calculates 5 random times to trigger a function to prompt the user with an open ai forted message, with intents ready to run, asking:

1. would you like to find which meme coins are hot on the markets today? we could find some gems early
- this triggers a findMemeGems functions which uses dextools, dexscreener, pumpfun and moonshot apis for trending tokens, then makes a list of all the tokens, prepares a formatted result to the user, whilst also saving the the list to a MongoDB document called userTodaysTrending saving each record by user id. Then it also saves iv another document called todaysTrending which this time saves using todays date as key and updates only missing entries to that days list of record entries. Each entry is a token Symbo and address and chain. 
- The user is presented with a user friendly result list by openai.
2. would you like to Buy this token. BuyMemeCoin intent then follows after results are returned from findMemeGems.
- If user agrees in text or voice, or sends confirmation in any form, the openai triggers a buy intent
- buy intent is meant to get user to confirm token address and amount to buy. Before firing the buyMemeCoin function in our bot
3. would you like to sell this token, To complement the buy intent we need a sell intent and function ready
4. would you like to review your positions? This triggers the monitorPositions function via the MONITOR_POSITIONS intent to check the user active swap positions. 
- it also calls the analze token function via the ANALZYE_TOKEN intent to scan its metrics of health in price and market interest from people.
- it then runs each meme token in the reuslt by address, using address as input to call the analyze token, in order to get a comprehensive view if the token is a good option to buy at this point tin time. It should have a criteria to rank tokens and identify which meme token is new and lucrative based on volume history, price history, holder growth, community size, etc. It also calls the scanToken function which is more of a security scan for a token checking if LP is locked, if token is bundled i.e. if deployer set a chuck of tokens aside on the contract create transaction. Just point to this transaction for very token address to see bundling.
- it then returns a comprehensive insight on each token 
5. would you like me to suggest some meme tokens? suggest intent, fires a function that fetches the users swap history from swapRecords document by user id, then fetches trending tokens history for those tokens versus the whole todaysTrendingRecords document  using getTrendingRankings to find the number of times each token saved in the trendingRecords document trended or showed up, then return a counted trending ranking for each token on the list of user traded tokens.
- it then returns some suggestions to the user and why its suggesting those. 
- it also goes on to run another call to analyze each trending tokens and then compile a list best on ranking of analysis. Then suggest only the top 5 as well. It returns result to user via open ai for formatting and with a FIND_GEMS intent in the result to trigger a fetch of the whole list of whats trending hot today.

We need to reply on MongoDB, documents:
- usersCollection this is our users databse they are saved in here when they register on first use, saving 2 wallets solana, ethereum and base wallet addresses. The structure looks like this:
- TrendingRecords
- userTodaysTrending 
- swapRecords (stores buy or sell transaction data for users by their telegram user ids (not usernames) callback.from
- autonomousOrdersRecords (stores buy or sell order data that will be executed at a certain time by the bot.
- add moree
- usersCollection document looks likt this for illustratin purposes in both writing requirements for the codebase functions in full scope:
const userDocument = {
      telegramId: userInfo.id, // Use telegramId
      username: userInfo.username,
      wallets: {
        ethereum: [{
          address: ethereumWallet.address,
          encryptedPrivateKey: ethereumWallet.privateKey,
          createdAt: new Date()
        }],
        base: [{
          address: baseWallet.address,
          encryptedPrivateKey: baseWallet.privateKey,
          createdAt: new Date()
        }],
        solana: [{
          address: solanaWallet.address,
          encryptedPrivateKey: solanaWallet.privateKey,
          createdAt: new Date()
        }]
      },
      settings: {
        defaultNetwork: 'ethereum',
        notifications: {
          showInChat: true
        }
      },
      registeredAt: new Date()
	autonomousMode:
    };
Functions not to forget:
- autonomousOrderExecutor (which runs every minutre, iterating through the mongoDB and checking if all order have been executed, if not it executes the order to buy or sell. Records are kept in swapRecords, and orders are fetched from and updated to autonomousOrders document.
- findMemeGems
- analyzeToken ( this runs a couple of api calls to providers to fetch token data, prices, token volume and lp metrics, community size of telegram, token trending past records from  TodaysTrending document.
- monitorPositions (which is called on behalf of or by user to get all order history and their state)
- createPosition (which creates a timed order record in the MongoDB autonomousOrders
- add moree 

I want it as a module folder split into multiple files with the entry being a file called openai.js located in the commands folder, where my start.js and index is. 
A user has to register first to use the bot.
we need to also factor in that when a user registers they have 3 wallets autol=matically created and their keys encrpyed and saved under their user id int the databse. Then the use has the option to go to settings and choose or set the wallet they want to enable for  if they want to enable automated trading or autonomous trading, which is checked first by the openai togther with the bot functions before proceeding to process a user prompt to buy or sell or set orders if there is not sufficient fnds in each scenario then it should tell user to enable or ask if they want to enable autonomusTrading. This is another intent ENABLE_AUTONOMINITY which means we also need a DISABLE_AUTONOMITY.
These trigger functions to enable or disabled automated ai trading. 

Its supposed to only run the ai when a user sends a voice note or when they type "hey katz" which can be typed loosely. Then openai will run a prompt asking a user what they would want to do today. Then list the 10 options we have. Alternatelvely can ask something else specific like say lets buy this token. 

We need a list of user prompts to be ready for that a meme investor can ask the bot, create a list based on the intents stated at the beggining of the document. Here are some more SUGGESTIONS TO EXPAND:
- A sentence or voice note containing the words "buy" for example lets buy $CHILLGUY. 
- Asentence or voice note containing the words "sell" for example lets sell $CHILLGUY
Users can send commands or prompts to the ai with all data already mentioned, like the amount to use to buy or amount to sell and the contract address. Unless the address was already provided in the chat. Then the openai triggers an intent called buyMemeCoin, which triggers a function in our TG Bot code called buyMeme.

We need functions to buy and sell any solana token, etheruem token and base token. This functiona can be triggered either manually or the bot can trigger it.

The buy or sell meme function should return a result of what just happened, then also generate a transaction certificate for the user. Can be a buy or sell success certificate for the specific transaction with amount used as input (eth or sol for buys), token amount for sells, the price the transaction was executed at, and the resulting tokens received.

When ever a person buys we also want to save the transaction in a MondoDB so we can keep track of profitability of the trade: time bought, price, amount bought. So that on sell when we can calculate profitability.

SellMeme function is supposed to save transaction data in the MongoDB. in a document called SwapRecords.

Certificates are drawn in canvas based on the relevant data to present to the user for a sell or buy. If its a swap - buy or sell, performed via the AI using voice or text, triggering intents that in turn end up running the buy or sell functions, we need to make sure we pass the certificate back to the openai and not message the certificate direct. This gives the calling function to pass a success message to open ai with the transaction details raw, whilst having the certifcate ready to send as message to user as soon as open ai finishes preparing its output. 

THE KILLLIST FOR THE BUILD GUIDANCE:
1. Utilize OpenAI's Realtime API: OpenAI's Realtime API enables developers to build low-latency, multimodal experiences, facilitating natural speech-to-speech interactions. This API combines speech recognition, language understanding, and speech synthesis, allowing for seamless voice interactions without the need for multiple models.
OpenAI

2. Implementing the Realtime API: To integrate the Realtime API into your application:

    Establish a WebSocket Connection: The Realtime API operates over WebSockets, enabling real-time audio streaming between your application and OpenAI's servers. This setup is crucial for maintaining low-latency communication.
    DataCamp

    Handle Audio Streams: Capture user speech input and stream it to the API. The API processes this input and returns synthesized speech, which your application can play back to the user. This bidirectional audio streaming facilitates natural conversational experiences.

    Manage Function Calls: The API supports function calling, allowing your agent to perform specific actions or retrieve information based on user requests. This feature is particularly useful for executing trades or accessing real-time data in a trading application.

3. Voice Selection and Customization: OpenAI has introduced five new voices—Ash, Ballad, Coral, Sage, and Verse—that offer greater expressiveness and can be tailored to your application's needs. You can select and customize these voices to align with your agent's personality and the user experience you aim to provide.
OpenAI Community

4. Cost Considerations: Be mindful of the associated costs when using the Realtime API. OpenAI has implemented prompt caching, which can reduce expenses by 50% for text inputs and 80% for audio inputs that hit the cache. Monitoring your usage and optimizing interactions can help manage costs effectively.
OpenAI Community

5. Integration with Trading Platforms: For executing trades, consider integrating your agent with trading platforms like Interactive Brokers using their API. By combining OpenAI's voice capabilities with trading APIs, you can create a voice-controlled trading application that processes user commands and executes trades accordingly.
Toolify

6. Development Resources: To assist in building your voice-enabled trading agent, several resources provide step-by-step guides and examples:

    DataCamp's Guide: Offers insights into setting up WebSocket connections and handling audio streams with the Realtime API.
    DataCamp

    Toolify's Tutorial: Provides a comprehensive walkthrough on creating a voice-controlled trading app using OpenAI's Whisper API and Interactive Brokers.
    Toolify

I need you to go through this draft, then get the sense of what being aimed to be built, then create a robust requirement list, including that for all functions and databses and intents. Then fill in whats missing to create the system and tie everything togther into a codebase. This will be our solid build plan, make it world class but optimized to help an AI understand whats being asked or what the full requirement list looks like when fed this plan.

I will then use this list to prompt an AI to write the codebase. Starting by defining folder structure, then it starts writing the files for each module one by one until we have a full working product based on the build plan.